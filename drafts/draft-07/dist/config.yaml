receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  # batch:
  simpleprocessor:
    prefix: "custom-"

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [simpleprocessor]
      exporters: [debug]
