receivers:
  otlp:
    protocols:
      grpc:
      http:


processors:
  sherlockunhealthy: {}


exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [sherlockunhealthy]
      exporters: [debug]